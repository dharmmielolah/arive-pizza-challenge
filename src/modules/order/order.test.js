import { render } from "@testing-library/react";
import Order from ".";

test("renders order module", () => {
  render(<Order />);
});

// test('other test', () => {
//   const linkElement = screen.getByText(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
// });
